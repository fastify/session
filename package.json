{
  "name": "fastify-session",
  "version": "0.2.3",
  "description": "a session plugin for fastify",
  "main": "index.js",
  "scripts": {
    "unit": "tap test/*.test.js",
    "test": "npm run lint && npm run unit",
    "lint": "./node_modules/eslint/bin/eslint.js index.js lib/* test/*",
    "coveralls": "npm run unit --  --cov",
    "coverage-report": "npm run coveralls && tap --coverage-report=lcov"
  },
  "keywords": [
    "session",
    "fastify"
  ],
  "author": "Denis Fäcke",
  "license": "MIT",
  "dependencies": {
    "cookie-signature": "^1.0.6",
    "fastify-plugin": "^0.2.1",
    "uid-safe": "^2.1.5"
  },
  "engines": {
    "node": ">= 6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/fastify-session.git"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "eslint": "^4.9.0",
    "fastify": "^0.37.0",
    "fastify-cookie": "^1.0.3",
    "request": "^2.83.0",
    "tap": "^11.0.0"
  }
}
